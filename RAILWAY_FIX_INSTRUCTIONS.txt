RAILWAY DEPLOY INSTRUCTIONS

1) Upload all files from this ZIP to GitHub.
2) In Railway, connect the GitHub repo.
3) Railway must use the Dockerfile. This repo includes railway.json with builder DOCKERFILE.
4) Add these Railway variables:

NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
NEXT_PUBLIC_APP_URL=https://your-app.up.railway.app
GAME_MASTER_EMAIL=xavinavarroros@gmail.com
NEXT_PUBLIC_GAME_MASTER_EMAIL=xavinavarroros@gmail.com

5) Redeploy.

The Dockerfile uses Node 20 and does NOT use the deprecated npm production config.
Email confirmation is bypassed through the server registration route, which requires SUPABASE_SERVICE_ROLE_KEY.
The GM panel is available at /gm. Register/login with the GAME_MASTER_EMAIL account.
